apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: bootstrap
spec:
  releaseName: team-awesome
  chart:
    spec:
      chart: namespace-bootstrap
      sourceRef:
        kind: HelmRepository
        name: bootstrap
        namespace: flux-system
      version: 0.1.0
  interval: 1h0m0s
  install:
    remediation:
      retries: 3